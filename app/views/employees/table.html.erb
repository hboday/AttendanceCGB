<% if current_user.employee? %>
<%= render partial: 'shared/tbl1', locals: {shift_assignments: @shift_assignments, employee: @employee, show_name: false}  %>
<% elsif current_user.hr? || current_user.manager? %>
    <%= render partial: 'shared/manager_tbl', locals: {shift_assignments: @shift_assignments, employee: @employee, show_name: false}  %>
<% end %>
